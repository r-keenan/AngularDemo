import { Component } from '@angular/core';

//selector is a our own custom HTML tag. in this case, it is <app-server></app-server>

@Component({
  selector: 'app-server',
  templateUrl: './server.component.html',
})
export class ServerComponent {}
